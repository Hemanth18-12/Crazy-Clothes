# Crazy Cloths ignored files
# NOTE: config.js and js/firebase-config.js are intentionally tracked and deployed
# They contain public Firebase client credentials (safe to commit per Firebase security model)
.DS_Store
Thumbs.db
